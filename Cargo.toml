[workspace]
members = [
    "crates/vortex-core",
    "crates/vortex-cli",
]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"
rust-version = "1.85"

[profile.release]
lto = "thin"
codegen-units = 1
panic = "abort"
strip = "symbols"
