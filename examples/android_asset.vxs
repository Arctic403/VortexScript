// Syntax fixture only. Real command names come from the host-provided schema.
transaction MobileAsset {
    command asset_import {
        asset "tree.glb"
    }

    command mesh_budget {
        mesh mesh:42
        triangles <= 20000
        background true
    }
}
